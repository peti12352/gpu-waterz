# N21 A3 emit domain

N21 diagnostic; not a throughput claim; not a 2 Gvox/s number; not 3090 Ti.

Skip. `ntab_use = next_pow2(ndirty*2+1024)` already. First inner
ntab=16,777,216 vs nuniq=4,439,124 (load ~0.26, design <=0.5). Not
`ntab >> nunique` in a broken sense. Do not resurrect `k_hash_clear`.
Product `EMIT_HOLES=1` stays. No kernel.
