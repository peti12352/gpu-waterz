# N21 W1 compress-subset

N21 diagnostic; not a throughput claim; not a 2 Gvox/s number; not 3090 Ti.

D0 no-go. p50 root_frac=0.29 (need >=0.5 and hops~1). e9b root_frac=0.55
with hops=0.90 would qualify alone; e9c root_frac=0.038. Compress-subset
cannot drop work on e9c. Packing non-roots is still an O(nlist) pass.
Hook list stays faces + phase-1 roots. Not implemented. Stamp
`N21_W1` / `N21_WS_FAT_LIST_STRUCTURAL`.
