# N16 T12 pinned changed

Not a 2 Gvox/s claim. Not 3090 Ti. `WATERZ_PIN_CHANGED=1` + T2+T5. Default 0.

`cudaHostAllocMapped` 4-byte `changed`; stitch loop `cudaDeviceSynchronize` then read `pin_host[0]` instead of D2H. `k_w5_hook_list` still `atomicExch(changed, 1)` (`ws.cu`). On a mapped pointer that is a PCIe atomic per successful hook.

## Gates

- ident: identity=True run2_array_equal=True nfrag=2175400 bg=506568 ndiff=0
  fp=fff9037cab341692be0c9bf3c577d4ff val peak=1928284592 leaked=0
  val nlist≈1.05e8; stitch still tens of ms (identity gate does not measure 2.16).
- 2.16 nlist=4.19e8 (e9b) / 2.85e8 (e9c). stitch_rounds=4/64 still, but:

| slab | e9b stitch ms | e9c stitch ms |
|------|---------------|---------------|
| 0 | 137357 | 206317 |
| 1 | 136724 | 204326 |
| 2 | 137403 | 206294 |

  WS=1028996.16 ms e2e=1031333 nlab=3860788 (partition lock holds; speed does not).
  cut vs T2T5 1311.4 = 0.0013.

## keep_default

False. Identity-true, ~780× slower on the graded volume. Kill. Do not default. Do not reuse mapped atomics for G2 D2H (T10).

`data/cache/n16_t12.json`
