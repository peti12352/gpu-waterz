# N16 T10 CUDA graph of G2 dirty body

Not a 2 Gvox/s claim. Not 3090 Ti. Not instantiated as a CUDA graph.

N15 killed this from nsys E-K=336 ms. N16 does not use that as the gate. GPU evidence:

1. `hash_combine_dirty` (`csrc/parhac_d.cu`) does `cudaMemcpy` D2H of `ndirty`/`nself` every inner, then a host branch, then optionally `k_count_live` D2H of `n_live`. `cudaStreamBeginCapture` of that function is illegal without deleting those copies.
2. T12 GPU-ran the mapped-host alternative for a 4-byte flag: val identity True, 2.16 WS=1028996 ms (atomicExch to BAR1 over nlist=4.19e8). Mapping the G2 D2Hs the same way is not a speed path.
3. T7 GPU-ran the remaining O(nscan) drop (`k_count_live` arithmetic) that a graph cannot replace: four-T PASS, T=0.3 VOI identical to N13, 2.16 agg=1930.1 still **> 1861.7**.
4. Deep stack (T2+T5+T6+T7) 2.16 agg=1929.0. Gap to 1.2x is 67 ms of kernel work, not launch overhead on a path that already deleted count_live.

keep_default=False. Graphing G2 cannot satisfy keep_default on this stack.
