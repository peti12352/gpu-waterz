# N17 T2 emit into rewrite holes

Not a 2 Gvox/s claim. Not 3090 Ti. Idle RTX 5090.
`WATERZ_EMIT_HOLES=1` + N16 deep (FOLD+SHARE_OFF+HOOK+FUSE_DIRTY+NLIVE_ARITH).
C++ default 0.

Replaces `k_vacate_kept` (O(nscan)) + compact emit + `k_fill_holes` with emit-into-keep-holes + tail zero. Hash path only; skips the small-ndirty RBK so val VOI actually exercises this kernel.

## Gates

- ident: True run2 True ndiff=0 nfrag=2175400 bg=506568
  fp=fff9037cab341692be0c9bf3c577d4ff val peak=1928284592 leaked=0
- T=0.3 VOI ε=0.40 2-run: split=0.44082269408145347 merge=0.25427477829766865
  nseg=321855 inner=538 merges=1853545 **identical to N13**
- four-T ε=0.08: `n15_four.py` four_pass True. Independent official
  `run_baseline.py` on `data/ws_bounty/n17_t2_four/`: **ACCURACY GATE: PASS**
  0.2 0.3707/0.3350; 0.3 0.4512/0.2505; 0.4 0.5162/0.2268; 0.5 0.6129/0.2184
  (same table as N16 T7/deep)
- 2.16 n8: agg=1688.95 nlab=3860788 e2e=3110.2 WS=1315.1
  vs N13 agg 2234.05 **cut=1.323** (gate 1861.71). vs N16 deep agg 1929 **-240 ms**

## keep_default

True for the **env lever** (ident + VOI + four-T + agg 1.2x + WS 1.2x).
C++ `emit_holes()` stays getenv 0 until 3090 Ti 24 GB peak is measured
(2.16 ws_peak still 13.22 GiB on 5090; SHARE_OFF is +4 B/vox).

`data/cache/n17_t2.json`
